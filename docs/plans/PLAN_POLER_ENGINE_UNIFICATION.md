# PLAN: POLER ENGINE — УНИФИКАЦИЯ И ДОРОЖНАЯ КАРТА

> **Дата:** 2026-09-22 · **Автор:** Super Z (исполнение) / Kotokvit (архитектура)
> **Основа:** директива владельца из сообщения 2026-09-22; циклы O (v0.50.0)
> и P (v0.51.0) закрыты. Мастер-промт: `docs/PROMPT_POLER_ENGINE.md`.

---

## 0. Резюме

Проведена верификация обоих крыльев системы — poler-engine (Rust, ядро
вычислений и квант) и P³ Engine (Zig, проективная геометрия и рендер).
Оба реальны, зрелы и математически совместимы: в P³ уже встроены
идемпотенты POLER (Eq.1 J=A−Aᵀ, Eq.8 Π_Λ), в poler-engine уже есть QCASM,
шумовые модели и анзац-механика. План сводится к трём циклам:
**Q** (квант: qcasm/QAOA/шум), **R** (мост в P³), **S** (кристаллы:
акустика и спектральные изображения) — с горизонтом на POLER-OS.

---

## 1. Верификационный отчёт

### 1.1. poler-engine v0.51.0 (org: poler-engine-org)

Проверено по git-истории, коду и релизам:

* Коммиты цикла P в main: `ed96fb5` (feat v0.51.0, 12 файлов, +3079/−23),
  `68dd878` (SKILL.md MCP-манифест). Релиз v0.51.0 с бинарником 18.4 МБ,
  сквозная E2E-проверка скачиванием пройдена.
* Тесты: **2529/2529** (pqc 495, engine 1566 и др.).
* Поверхность `quantum`/`qm`: run / teleport / bloch / state / verify /
  calc / list (+ --json); MCP-инструмент `poler_quantum` (всего 32
  инструмента); CLI `pqc verify` (unitary / equiv / teleport).
* Верификатор `crates/pqc/src/verify.rs` (~1000 строк): градуированные
  вердикты, точное кольцо ℤ[1/√2, i] для Clifford+T (Cp(θ=k·π/4)),
  MAX_EXACT_QUBITS=8 / MAX_NUMERIC_QUBITS=10, далее — сэмплирование.
* Готовые заготовки цикла Q (проверено чтением кода):
  * `qpc.rs` — **QCASM-lite**: `qubits N`, `h|x|y|z|s|sdg|t|tdg q`,
    `cx|cz|swap`, `ccx`, `cp θ`, `ry|rx|rz`, `flipphase/flipzero`, `measure`.
  * `noise.rs` — MCWF-траектории: деполяризация, амплитудное затухание
    (T1), фазовое затухание (Tφ = T2 − T1/2), ошибка чтения; пресеты
    `ibm-heron`, `google-willow`, `noisy-90s`.
  * `ansatz.rs` — два движка: statevector (≤ 2²⁰ амплитуд = 16 MiB по
    умолчанию) и LENS-разреженный product-анзац ⊗R_y(arccos p̂) без
    материализации 2^d.

### 1.2. P³ Engine (личный аккаунт Kotokvit — подтверждено: вне орг)

Клонирован и изучен `Kotokvit/P3_Engine` (HEAD `641962a`):

* **83 файла в src/, 38 708 строк Zig** (`wc -l src/*.zig`), 674 теста
  только в src/ (README заявлял 59 модулей / 36 455 строк / 627+ тестов —
  база выросла: O3DE-нативная интеграция, UI-стек, скелетная анимация,
  транспортная физика).
* Математическое ядро проверено чтением: `p3_kernel.zig` (HomVec4, PGL4,
  FS-метрика), `p3_algebra.zig` (Грассман, Плюккер, Hodge), `p3_geodesic.zig`
  (S³/RP³, RK4, параллельный перенос), `p3_idempotent.zig` (**POLER Eq.1
  J=A−Aᵀ и Eq.8 Π_Λ — мост уже в коде**), `p3_causal.zig` (P³×R, световые
  конусы d_FS − |Δt|/c_eff, подпись «Архитектор: Kotokvit, исполнение:
  Super Z»).
* Vision-контур: `p3_vision.zig` (~1234 LOC) — софт-растеризатор с тройным
  буфером **RGB + depth + segmentation** (8-bit entity ID), zero-singularity
  по горизонту W=0; `p3_closed_loop.zig` — LLM-агент читает action-script,
  рендерит кадры, публикует PNG+depth+seg+meta. Это и есть «зрение без
  GPU» — фундамент для математической генерации изображений.
* Свежая история: `641962a` (launcher, пофазовое логирование), `7f88ba7`
  (SIGSEGV-фикс), `5422c6e` (PBR Schlick, source-build green),
  `fdb2de5` (O3DE-нативная интеграция).
* **Ограничение верификации:** в текущей песочнице нет Zig-тулчейна —
  сборка/тесты не повторены; опираемся на исторический отчёт
  (ГЛУБОКИЙ_АНАЛИЗ_P3_ENGINE.md, 2026-08-15: 43/43 шагов сборки,
  773/773 тестов) и статический анализ кода. Первое действие цикла R —
  воспроизвести `zig build test` на машине с Zig 0.14.0.

### 1.3. Логи: путь tar-в-tar → POLER BOX → генерация изображений

Из `docs/chat_dialogue.md` (10 МБ) и `docs/conversations/` восстановлено:

* Архивная экосистема владельца содержит вложенные конструкции
  (`P3_Engine_repo.tar.gz`, `poler-unified-source.tar.gz`,
  `Etheria_7_EPUB_Collection.tar.gz` внутри `POLER-DYNAMIS-v5/…`);
  research-архив `omni_vision_streaming_sources.tar.gz` (30 МБ,
  vllm-omni и др.) живёт прямо в `docs/research-archive/`.
* На этой базе развился **POLER BOX**: `src/boxenv/` (seccomp),
  gateway-контейнеры v0.27.0 (root-broker «sudo как услуга»,
  jailbreak-sentinel с ~28 векторами побега, канарейки) и
  `src/archive/` (CDC-дедуп, zero-copy reader, stream_writer).
* Директива владельца (2026-09-21): капсула-архиватор, в которой код
  распаковывается и компилируется прямо внутри архива + встроенный
  браузер для скачивания научных работ.
* **Смысл для генерации изображений:** связка «архивная математика
  (чанкование/дедуп/low-rank) + omni_vision референсы + P³-растеризатор»
  открывает класс изображений-как-функций (детерминированный синтез из
  спектров и полей вместо стохастической диффузии) — трек S2.

### 1.4. Честные границы (фиксируются в документации каждого цикла)

1. GHZ-1024 за 367 мс — **стабилизаторный формализм** (Готтесман–Книлль),
   18 ПБ статектора не материализуются. Произвольные схемы: statevector
   ≤ ~20–26 кубитов (16 MiB–1 GiB); далее — сэмплирование/продукт-анзац.
2. «Симуляция галактик/организмов» — направление, а не текущая
   способность. Реалистичные вехи: N-body Барнса–Хата, сеточные PDE,
   спектральный анализ — каждый со своим паспортом производительности.
3. Акустический кристалл и спектральные изображения (цикл S) —
   исследовательский трек с промежуточными верифицируемыми артефактами
   (SVD-кодек изображения, реконструкция формант), а не готовый продукт.

---

## 2. Целевая архитектура

```
┌─────────────────────────────────────────────────────────────────────┐
│ СЛОЙ 4 · ПРИМЕНЕНИЯ: акустика (S1) · изображения (S2) · игры на P³  │
├─────────────────────────────────────────────────────────────────────┤
│ СЛОЙ 3 · МОСТЫ: p3_bridge C-ABI · MCP-шлюз · SMT-верификатор · BOX  │
├─────────────────────────────────────────────────────────────────────┤
│ СЛОЙ 2 · КВАНТ pqc: QCASM · стабилизатор · QAOA · шум · verify       │
├─────────────────────────────────────────────────────────────────────┤
│ СЛОЙ 1 · ГЕОМЕТРИЯ P³: PGL4 · FS · Грассман · геодезические · зрение│
├─────────────────────────────────────────────────────────────────────┤
│ СЛОЙ 0 · ЯДРО calc: ℂᴺˣᴺ · eigen · expm · schrodinger · идемпотенты │
│                     рантайм будущего: POLER-OS (Zig, Ring 3)        │
└─────────────────────────────────────────────────────────────────────┘
```

Принцип: один математический базис (матрицы, спектры, проективная
метрика) обслуживает все слои; каждый мост проходит конформанс-тесты.

---

## 3. ЦИКЛ Q «Анзац и Шум» (v0.52.0) — следующий

Цель: замкнуть квантовый стек — произвольные схемы из шелла, оптимизация
ансатца, шум поверх верификатора. Всё строится на готовых модулях
(qpc.rs, noise.rs, ansatz.rs, verify.rs).

* **Q1. `quantum run qcasm`** — запуск произвольных QCASM-схем из шелла
  и MCP: `quantum run qcasm circuit.qc` + heredoc/строка; отчёт
  (гистограмма, амплитуды при --state, точный режим при Clifford+T);
  интеграция в реестр AlgoParams/catalog; `pqc run --qcasm`.
* **Q2. QAOA-ансатц с оптимизацией** — `quantum qaoa --problem
  maxcut|portfolio --p N`: cost-гамильтониан → анзац
  [H_d, U(H_C,γ)]×p → классический оптимизатор (coordinate descent +
  parameter-shift градиенты; без внешних зависимостей) → отчёт
  (энергия по итерациям, лучший битстринг, аппроксимационное отношение).
* **Q3. Шум поверх верификатора** — конвейер verified → noised:
  сначала вердикт верификатора (unitary/equiv), затем прогон той же
  схемы через noise.rs с пресетом железа; отчёт о фиделити
  (ideal vs noisy, χ² гистограмм); `quantum verify ... --noise
  ibm-heron --shots 4096`.
* **Приёмка:** тесты (включая property-based на случайных схемах),
  дамп `docs/benchmarks/QUANTUM_QAOA_NOISE_DUMP_v0.52.0.md`, релиз с
  бинарником, пуш каждого зелёного шага.

## 4. ЦИКЛ R «P³-Мост» (v0.53.0)

Цель: живое соединение ядра и движка.

* **R1. Воспроизведение P³** — `zig build test` на машине с Zig 0.14.0
  (в песочнице нет тулчейна); фиксация версии в docs.
* **R2. C-ABI конформанс** — `p3_bridge.zig` экспортирует FS-метрику и
  PGL4-действия; Rust-сторона (cdylib) прогоняет те же тесты:
  d_FS(a,b) совпадает в пределах f64-честности; U†U=I для PGL4-элементов;
  идемпотенты P²=P обеих сторон на одинаковых матрицах.
* **R3. Демо «кадр из гамильтониана»** — POLER считает эволюцию
  (schrodinger/спектры), P³ рендерит её в тройной буфер (RGB+depth+seg),
  headless PNG на выходе. Первый публичный артефакт «изображение,
  просчитанное математически».
* **Приёмка:** конформанс-отчёт + демо-PNG в docs, релиз.

## 5. ЦИКЛ S «Ядро Игры» (v0.54.0) — выполнен

> **Рескоп 2026-09-23 (директива владельца):** владелец состоит в
> организации Epic Games и может форкать исходники Unreal Engine;
> POLER ENGINE изначально задумывался как игровой движок. Прежде чем
> копировать чьи-то решения, нужен честный разбор недостатков
> существующих движков — см.
> [`docs/GAME_ENGINE_ROADMAP_UE_ANALYSIS.md`](../GAME_ENGINE_ROADMAP_UE_ANALYSIS.md)
> (анатомия UE5, 10 болей UE + Unity/Godot, 8 паттернов UE что берём,
> Rust-ответы, роадмап G-циклов S→W, список файлов UE для изучения
> в форке). Исследовательский цикл «Кристаллы» сдвинут в T.

* **S1. World/Entity/Transform** — `src/game/world.rs`: сущности с
  поколениями (генерационные слоты — нет GC и висячих ссылок
  конструктивно), плотные компонентные хранилища (Transform/Orbit/Body),
  иерархия parent→children с DFS-порядком, despawn поддерева.
* **S2. Fixed-timestep цикл** — `loop_.rs`: аккумулятор, целые тики
  только, cap на догоняние (спираль смерти не разгоняется),
  `alpha()` для будущей интерполяции рендера.
* **S3. Кеплер-лайт физика** — `orbit.rs`: ω = √(G·M)/r^1.5 из массы
  родителя (третий закон Кеплера — никакой бутафории скоростей),
  наклоны орбитальных плоскостей, state-hash FNV-1a по битам позиций
  (детерминизм бит-в-бит — фундамент replay/lockstep).
* **S4. Сцены** — `scene.rs`: serde-JSON (топологический порядок
  объявлений, валидация на границе), демо-сцена «Этерия» (11 тел,
  глубина иерархии 3), `game write-demo` для редактирования.
* **S5. Рендер мира через P³** — `render.rs`: каркасы сфер (3 кольца ×
  спин), кольца орбит, нормализация данных к ±0.92 (защита афинной
  карты W-доминированием), full-bleed бокс, аннотация (5×7 шрифт,
  легенда, state-hash). VLM-ревью 3 итерации: 6→7.5→**9/10 SHIP**.
* **Приёмка:** 35 тестов game (+ шелл), бенчмарк
  `docs/benchmarks/GAME_CORE_v0.54.0.md` (2.7 млн тиков/с, тик 0.37 мкс
  = 2% бюджета 60 Гц кадра), демо-PNG в `docs/demo/game_core_v0.54.0/`,
  релиз.

## 6. ЦИКЛ T «Кристаллы» (v0.55.0 — ВЫПОЛНЕН)

* **T0. Полер-бокс** — UE-заголовки владельца (коммит 266477d)
  перегнаны в `docs/research-archive/ue_reference.poler` (родной
  контейнер, ratio 0.21); `.poler`/`.t5z` встроены в `ArchiveKind`
  → `--archive-list` / `grep --archives` / `--poler-cat` видят его
  прозрачно; аналитика `POLER_NOTES.md` добавлена **внутрь** архива
  in-place CoW-патчем; сырые копии удалены из git.
* **T1. Акустический кристалл — игровой звук** (`src/game/audio.rs`):
  сонофикация мира физикой — ω = √(GM)/r^1.5 → высота (до 3 октав,
  полутоновая карта), радиус → гейн, X → power-pan. ADSR, 4 формы
  волны (класс тела → тембр), мягкий tanh-лимитер, свой WAV PCM16,
  свой радикс-2 FFT; STFT 4×4096 → топ-6 пиков → `crystal_hash`.
  Контракты: `audio_hash` бит-в-бит (аналог state_hash). Шелл:
  `game sound --ticks N --out F.wav`. Артефакт: демо «Этерия» 15 с,
  11 голосов, peak −1.1 dBFS.
* **T2. Спектральный синтез — процедурные текстуры**
  (`src/game/texture.rs`): текстура = аналитическая функция (value
  noise + quintic-сглаживание, fBm до 12 октав, domain-warp) →
  бесконечный зум без пикселизации; стили noise/marble/wood,
  палитры gray/copper/ice/jade, контраст. SVD rank-k кодек
  (односторонний Якоби–Хестенес, тайл 16): rank 4/16 = 55–57 dB
  (текстура — низкоранговый объект). `texture_hash` бит-в-бит.
  Шелл: `game texture --style … --rank-curve`. VLM: marble 8.5,
  noise 7.5, wood 7 — все SHIP.
* **Приёмка:** артефакты (WAV + 3 PNG) + метрики (PSNR/ранг-кривая,
  dBFS, хеши) + дамп `docs/benchmarks/GAME_CYCLE_T_v0.55.0.md`.

## 7. ЦИКЛ U «Окно» (v0.56.0 — ВЫПОЛНЕН)

* **U0. Normal maps из той же функции шума** (`src/game/texture.rs`):
  тангентные нормали честной центральной разностью dh/du (оценка не
  зависит от разрешения/зума), амплитуда = глубина рельефа в долях
  тайла; RGB = n·0.5+0.5. VLM: marble 8, wood 8, noise 7.5 — SHIP.
* **U1. Система событий** (`src/game/events.rs`): типизированные
  `Event` (окно/клавиши/мышь/колесо/текст/user), очередь с
  коалесцингом (MouseMove — суммированием, Resize/Focus —
  keep-last), drop-oldest при переполнении + compact-проходка,
  `events_hash` FNV-1a — контракт replay.
* **U2. Свёрнутый ввод** (`src/game/input.rs`): клавиатура — битовая
  карта 128 клавиш (два u64), рёбра `just_pressed`/`just_released`
  живут ровно кадр, автоповтор не порождает рёбер; мышь — кнопки +
  дельты + колесо; текст-буфер; `input_hash`. `ActionMap` —
  семантические биндинги с ИЛИ-семантикой.
* **U3. Оконные бэкенды** (`src/game/window.rs`): трейт
  `WindowBackend` (poll_event/present/size/is_closed);
  `OffscreenWindow` — детерминированный рекордер (события из скрипта,
  кадры → PNG + `frames_hash`); `X11Window` — настоящее окно Linux
  через прямой dlopen libX11.so.6 (как P³-мост: extern "C" + dlsym,
  zero-dep), маски KeyPress/…/StructureNotify/Focus, колесо =
  кнопки 4/5, WM_DELETE_WINDOW, XImage-ZPixmap блит RGB→BGRA.
* **U4. Орбит-камера** (`src/game/camera.rs`): yaw/pitch/dist с
  целями и экспоненциальным демпфированием (кадронезависимо при
  фиксированном dt), ЛКМ-драг → орбита, колесо → мультипликативный
  зум с клампами, WASD/стрелки/QE — безмышиная альтернатива;
  `camera_hash` бит-в-бит.
* **Единый игровой цикл** (`game_window_loop`): события → Input →
  тики мира+камеры (fixed dt) → `render_raw` → present. Один код —
  офлайн-replay (dt = FIXED_DT) и realtime X11 (dt по Instant с
  пейсингом 60 Гц).
* **Шелл:** `game normalmap`, `game input-demo` (--script JSON:
  key/mouse/wheel/text/resize/focus/close/user по кадрам), `game
  window` (в headless — внятный отказ).
* **Приёмка:** 36 новых тестов (события 7, ввод 10, окно 6, камера 7,
  normal maps 3+3 шелл); детерминизм: двойной прогон input-demo →
  идентичные frames_hash/camera_hash/state_hash; производительность:
  600 кадров 640×360 за 1.5 с (2.5 мс/кадр, ~400 fps офлайн, 6.7×
  запас к 60 Гц); VLM: кадры орбиты 9/10 SHIP (заметка: input-оверлей
  в аннотацию — бэклог V). Дамп
  `docs/benchmarks/GAME_CYCLE_U_v0.56.0.md`.

## 8. Горизонт (G-циклы после U)

* **POLER-OS как рантайм:** нативный запуск единой системы (микроядро
  уже поднимает PE64/Linux POSIX, VirtIO, FAT32 RW).
* **Капсула-архиватор:** `.poler`-контейнер, внутри которого код
  распаковывается, компилируется и тестится + встроенный headless-браузер
  для научных загрузок (директива 2026-09-21).
* **GPU-этап:** Vulkan/NVK + WGSL-шейдеры P³ (p3_gpu_rt.zig уже
  содержит Dawn/WebGPU-рантайм).

> **Директива владельца (2026-09-22, файл-план):** «Всегда можно накатить
> Ubuntu внутри POLER BOX — оно сжимает всё, даже ПО и ОС, в реальном
> времени». Значит: любой недостающий тулчейн (Zig для цикла R, GPU-стек)
> разворачивается в контейнере POLER BOX с образов Ubuntu, а слой
> `src/archive` (CDC-дедуп + сжатие) обслуживает образы в реальном
> времени. Дефицит песочницы ≠ блокер.

## 9. Риски и меры

| Риск | Мера |
|---|---|
| Расхождение Rust↔Zig математики | R2-конформанс на общих фикстурах, golden-файлы |
| Комбинаторный взрыв QAOA | statevector ≤ 20q, product-анзац далее, честные лимиты в CLI |
| Отсутствие Zig/GPU в песочнице | R1 выполняется на машине владельца; софт-растеризатор как fallback |
| Раздувание кристаллов S | идемпотентная конденсация + дампы размеров на каждой вехе |

## 10. Definition of Done (каждый цикл)

1. Тесты зелёные (workspace + P³, где применимо).
2. Дамп производительности в `docs/benchmarks/`.
3. Релиз с бинарником + сквозная проверка скачиванием.
4. Worklog-запись + пуш; границы честности зафиксированы в docs.
