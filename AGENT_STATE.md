# AGENT STATE — машиночитаемое состояние агента (poler-engine)

> Обновляется в конце каждой сессии и после значимых коммитов.
> Истинный HEAD — `git log -1 --oneline`. Формат — строгий `key: value`.

updated_utc: 2026-08-30T09:40:00Z
repo: poler-engine
branch: main
commit: HEAD сессии v0.21.0 Hardening & Precision Upgrade (4 задачи владельца: CodeSymbolIdentity, Triage Layer, Semantic Bridge, Benchmark Suite)
tag: v0.17.6 (релизные теги — не на каждый патч; v0.18.0 License Gate; v0.19.0 Browser Surface; v0.20.0 Native Retrieval; v0.21.0 Hardening & Precision)
pushed: true
remote: https://github.com/poler-engine-org/poler-engine.git (репо ПЕРЕНЕСЁН в оргу poler-engine-org 2026-08-29; старые URL Kotokvit/* редиректят; второй репо орги — poler-engine-org/POLER-Quantum-RS, тоже private)
tests: cargo test 783 пройдено (744 lib + 38 integration + 1 doc, 0 failed, 4 ignored=live; +37 к v0.20.0); E2E live v0.20.0: полнота против GNU grep на src/ — 1669=1669 строк («fn »), 17=17 (web_tokenize), 4=4 (кириллица «полнот»); скорость release: 23мс против 18мс GNU grep (маленькое дерево, разница = rayon-стартап + полный отчёт в памяти); exit-коды 0/1/2 живые; --grep-json c byte_offset/ranges; --chunk FUTURE_ROADMAP.md = 25 чанков/2434 токена с breadcrumbs; MCP: tools/list 9 инструментов, poler_grep/poler_chunk отвечают
current_task: v0.21.0 Hardening & Precision Upgrade — РЕАЛИЗОВАН (4 задачи владельца без расползания фич): (1) строгий CodeSymbolIdentity в EntityGraph — IdentityPolicy::Text (case-insensitive, как было) | IdentityPolicy::CodeSymbol (точный регистр + квалификация module::path; Foo/foo/FOO = три символа; parser::parse ≠ cli::parse; find_code_symbol с суффикс-резолвом при уникальности; add_code_triple с дедупом рёбер; EntityGraph::from_symbol_table строит граф символов из AST-скана с приоритетом file-local разрешения имён; display_name не повышает регистр для кода; 12 новых тестов Rust/Python/JS); (2) Triage Layer в AIDDE — SIDE_EFFECT_MARKERS → официальная таблица TRIAGE_MARKERS с категориями (MemorySafety/Concurrency/GlobalState/Io/Network/ProcessControl/Output/Panic), triage_scan() возвращает TriageAlert{marker,description,category}, ImpactPassport разделён: structural_relations (ДОКАЗАНО call graph: upstream+downstream) vs heuristic_triage_alerts (эвристический СИГНАЛ, не доказательство), danger level считается ТОЛЬКО по структурным отношениям, JSON-форма паспорта изменена (side_effects больше нет), shell-вывод помечает proof vs heuristic; (3) Semantic Bridge — src/retrieval/semantic_bridge.rs: трейт SemanticSensor (внешний датчик близости, подключается без правки ядра — тест-мок нейросети подтверждает контракт), LexiconSensor = офлайн-словарь ~150 пар ru↔en в стем-пространстве индекса (прямое ru→en conf 0.90/0.70, обратное en→ru ×0.85), SemanticBridge::expand → QueryExpansion с WHY (provenance + confidence + вес BM25 = 0.85·conf ≤ BRIDGE_TERM_WEIGHT), WebIndex::search отрефакторен в search_core с ВЗВЕШЕННЫМИ термами (родные 1.0, сенсорные ≤0.85, title-буст не разбавляется кандидатами), search_with_bridge возвращает (hits, expansion); интеграции: --web-search автоматически через мост (WHY в stderr/md/json), новый флаг --semantic-expand (диагностика расширения), MCP poler_web_search и shell web-search с WHY-блоком; боль §8.1 ЗАКРЫТА: «блокировка мьютекса» находит EN-only mutex-документ (без моста 0 хитов) + живой прогон на web-index.db docs.rs нашёл lock/mutex-страницы; (4) Benchmark & Regression Suite — src/bench/mod.rs + CLI --benchmark/--benchmark-json: контур 1 Exact Retrieval (POLER grep vs ripgrep/grep: медиана latency + ПОЛНОТА parity, корпус 300×120 с посаженным NEEDLE), контур 2 Explainable Lexical (BM25/WebRank + мост: индексация 400 страниц, 10 golden-запросов RU/EN/mixed с проверкой top-1), контур 3 Passage (POLER Chunker vs naive-сплиттер: целостность предложений + latency), контур 4 Resources (VmHWM/VmRSS из /proc/self/status); детерминированные корпуса на xorshift-ГПСЧ; 9 golden-тестов в cargo test; живой прогон: POLER grep 3.3мс vs ripgrep 6.3мс при parity 195=195 строк, чанкер 100% целостности vs 2.1% naive, RAM пик 10.8MB; БОНУС-ФИКС чанкера: бенчмарк вскрыл v0.20-дефект — жадная сборка при абзацах ≈ target оставляла пустые «

»-чанки и микро-чанки-заголовки < min_tokens — пост-проход слияния (пустые выбрасываются, микро приклеиваются к следующему куску секции, инвариант среза сохранён)
current_task_note: семантика разделения доказательств и эвристик — урок аудита v0.20: смешанный список side_effects выглядел одинаково достоверным для агента; теперь типовая система не даёт смешать proof и heuristic; Semantic Bridge = владелец явно вернул задачу приказом v0.21 (архитектура НЕ через кристалл POLER-Quantum-RS — сенсор подключается трейтом, ядро детерминированное; LexiconSensor офлайн, ноль зависимостей); весовая политика моста: родной терм всегда весит больше кандидата сенсора (1.0 vs ≤0.85), фразы «"..."» сенсором не расширяются; чанкер-фикс: merged.tokens < target + min_tokens (ограниченный перелимит вместо мусорных чанков); тесты 746→783 (+37): 12 graph, 5 impact/triage, 12 bridge, 9 bench (+1 интеграционный переписан под новый паспорт)
next_task: сопровождение Dogfooding; владелец утром тестировал WebLens в браузере (v0.19.0/v0.20.0 приняты: «колоссальный скачок»); litnet: --google-fetch /account/notice возвращает анонимную витрину — для доступа к аккаунту один ручной логин через --google-browse на машине владельца; кандидаты: расширение словаря LexiconSensor (частотный анализ корпуса + пользовательские пары через конфиг), внешний ИИ-сенсор по трейту SemanticSensor (локальная модель), Firefox-WebLens тем же MV3-кодом, большой прогон полноты poler-grep на ядре Linux (~25к файлов), кросс-язычный метрический замер по бенчмарку при каждом релизе (--benchmark-json как baseline-файл)
blocked_on: ничего (для рут-коза висячки нужен один успешный consent-прогон под strace — требует тапа владельца)
tmux_sessions: нет (контейнер без root; на сервере обязателен tmux — §2 канона)
credentials: ВАЛИДЕН — файл-хранилище upload/«гитхаб токен .txt» (API 200, перепроверен 2026-08-29); подача через /home/z/my-project/scripts/gh-cred-helper.sh; автопроверка — agent_bootstrap.sh (канон)
notes: канон протокола Context-Free Resilience — POLER-Quantum-RS v0.3.8 (AGENT.md); workspace-репо /home/z/my-project/.git локальное, НЕ пушить; dev-stand живёт в песочнице /home/z/my-project (стенд = копия в dev-stand/, синхронизировать при правках); gcp-live.json/gcp-oauth.json/client_secret.json — только 0600 в ~/.config, в git не попадают

## Последние сессии

| Дата (UTC) | Задача | Результат |
|---|---|---|
| 2026-08-30 | v0.21.0 Hardening & Precision | CodeSymbolIdentity (Foo≠foo≠FOO, module::path, from_symbol_table) + Triage Layer (proof vs heuristic в ImpactPassport) + Semantic Bridge (офлайн ru↔en сенсор, вес ≤0.85, WHY, §8.1 закрыта: рус запрос находит EN корпус) + Benchmark Suite (--benchmark, POLER grep 3.3мс vs ripgrep 6.3мс parity 195=195, чанкер 100% vs 2.1%, RAM-снимок) + фикс чанкера (пустые/микро-чанки); 783 теста; ни одной новой зависимости |
| 2026-08-30 | v0.20.0 Native Retrieval | Слой 0: grep-режим (полнота = GNU grep 1669=1669 живьём, контекст/счётчики/exit-коды/JSON с byte-offsets) + слой B: RAG-чанкер (иерархия уровней, перекрытие, инвариант точного среза, breadcrumbs) + poler_grep/poler_chunk в MCP (9 инструментов); ни одной новой зависимости; gap-анализ GNU grep/text-splitter/LangChain в docs/native-retrieval-analysis.md; 746 тестов |
| 2026-08-29 | v0.19.0 Browser Surface | 4 фикса краулера (автодетект playwright, пер-страничный таймаут, robots-сообщения, CDP-самовосстановление) + --browser-index + WebLens MV3 (вшит в бинарник; --web-lens = автоустановка через --load-extension + MCP-демон; --web-lens-install для своего браузера); preflight-фикс MCP-HTTP; poler_crawl respect_robots/page_timeout_ms; 703 теста; все E2E живые |\n| 2026-08-29 | web-ux-audit | Живой аудит веб-конвейера (Dogfooding): замеры краул/поиск/REPL; 4 точки боли; FUTURE_ROADMAP §8 — тиры T0-T3 браузерной поверхности: WebLens = MV3-расширение поверх существующего --mcp-http (Bearer+CORS готовы), форк Chromium отклонён с цифрами; дефляция маркетинга (кросс-язычности нет — замер 0 результатов) |
| 2026-08-29 | org-transfer | Оба репо перенесены в poler-engine-org (HTTP 202, private=true сохранён, admin/push на месте, редиректы Kotokvit/* работают); орга: 1 участник, 2 private/0 public, default_perm=read; private-only-политика недоступна на free (422, некритично); origin переключён на оргу; ссылки README/AGENT.md обновлены; стратегия Dogfooding + MoR-планы зафиксированы в FUTURE_ROADMAP.md §7 |
| 2026-08-29 | license-gate v0.18.0 | License Gate ed25519 (PO1): тиры+trial+квоты+grace, license-tool, гейты CLI/shell/MCP, --license/--license-import, прозрачность NotebookLM в --google-auth; 687 тестов; E2E: активация/подделка отклонена/квота блок/live gmail под pro; секрет-скан PASS; лицензия владельца pro до 2036 |
| 2026-08-29 | monetization-strategy | Репо poler-engine + POLER-Quantum-RS переведены в PRIVATE (0 форков/0 релизов — потери нет, обратимо). Факты Google: скоупы движка оба restricted; CASA Tier 2 = $540/год (TAC); Testing-режим = 100 юзеров + 7-дневные refresh. Решение: платная закрытая бета БЕЗ верификации → License Gate (ed25519) следующим шагом; верификация отложена до платящих юзеров |
| 2026-08-29 | oauth-врезка | v0.17.7: --google-auth на живом клиенте POLER Engine — E2E пройден дважды (токены 0600, аудит, --google-status/--google-gmail/refresh живые); hard-exit патч + shutdown браузера; poler-auth-selftest.js/fakecode.js в dev-stand; cargo test 667 зелёные; запушено |
| 2026-08-29 | gcp-e2e | E2E OAuth-тест ПРОЙДЕН: consent → auth-code → token exchange 200 → refresh 200; brand «POLER Engine» + Desktop-клиент + Drive/Gmail API подтверждены через API (gcp-verify-state.js); набор gcp-*-скриптов (cdp-machinery, e2e, newclient, branding-audience, secret, verify-state) в dev-stand; запушено |
| 2026-08-29 | gcp-setup | gcp-setup.js + daemon в dev-stand: цифра подтверждения в лог+превью, одна попытка 61 мин без рестартов; подтверждение 79 пройдено, auth-code пойман; token exchange 401 invalid_client (креды gcloud SDK) — блокер; секреты вынесены в 0600-конфиг; запушено |
| 2026-08-29 | dev-stand | headless-стенд Auth Companion: CDP-релей превью + супервизор + security-audit (27/2/0); вход Google подтверждён (28 cookies, exit=0); коммит 741c5ae, запушено |
| 2026-08-28 | agent-протокол | AGENT.md-стаб + AGENT_STATE.md (канон: POLER-Quantum-RS v0.3.8) |
| 2026-08-28 | sync-remotes | merge 36b825f двух линий v0.17.4, 601 тест, запушено |
| 2026-08-28 | Transcript View | F3-лента + Response View, PTY-смоук PASS, c7a1d86 |
