set timeout=1
set default=0

# Force text mode (80x25) — no graphical framebuffer
# This ensures VGA text buffer at 0xB8000 is active when kernel starts
set gfxpayload=text

menuentry "POLER-OS v0.7.0 (64-bit, Ring 3)" {
    multiboot2 /boot/poler-os64
    boot
}
