export const metadata = {
  title: 'poler-engine · Auth Companion',
  description: 'Интерактивное окно авторизации Google — poler-engine v0.17.6',
};

export default function RootLayout({ children }) {
  return (
    <html lang="ru">
      <body>{children}</body>
    </html>
  );
}
